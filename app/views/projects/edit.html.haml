= render :partial => "form", :locals => {:project => @project}
- content_for :context_navigation do
  %h2= @project.name
  %p= link_to 'Show details', @project
  %p= link_to 'Delete this project', @project, :confirm => 'Are you sure?', :method => :delete